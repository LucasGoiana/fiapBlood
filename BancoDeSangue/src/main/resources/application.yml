server:
  port: ${port:8080}

client:
  nominatim:
    baseUrl: https://nominatim.openstreetmap.org/search
    formatData: format=json
    limit: 1
  viacep:
    baseUrl: https://viacep.com.br/ws/%s/json/

spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bancoDeSangua?createDatabaseIfNotExist=true
    username: lucas
    password: 123456
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true